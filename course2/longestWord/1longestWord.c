#include <stdio.h>
int main(void) {
    // declare all nessasary variables
    int numofwords = 0;
    int longestword = 0;
    int i = 0;
    // scan number of words
    scanf("%d", &numofwords);
    printf("numofwords = %d\n", numofwords);

    // start a while loop
    char words[101];
    while(longestword != '\0') {
        longestword = longestword + 1;
    }
}