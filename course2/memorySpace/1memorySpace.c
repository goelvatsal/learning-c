#include <stdio.h>
int main(void) {
    // scan the num of tracking codes
    int numofCodes
    // set up if statement for each letter
    // use sizeof() function
    // printf the num of bytes used by the tracking code
}