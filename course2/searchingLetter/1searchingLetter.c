#include <stdio.h>
int main(void) {
    // declare nessasary variables
    int tfound = 0;
    char wordscanned = 0;
    char word[51];
    int foundornot = 0;

    // create if statement inside while loop
    int i = 0;
    while(!foundornot && i < 51) {
        if(item == t)
    }
    // search for letter T or t
    // print 1 for t in first half; 2 for t in second half; -1 for no t at all
}